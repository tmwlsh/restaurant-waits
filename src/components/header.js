import { Link } from "gatsby"
import React from "react"

const Header = () => (
  <header>
    <div className="container">
      <Link to="/">Restaurant Wait</Link>
    </div>
  </header>
)

export default Header
